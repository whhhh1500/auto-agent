# Historical plan — superseded

This plan is archived for traceability and is not an implementation or
deployment checklist. The current Windows path is the ordinary current-user
Basic runtime described in [`docs/runner.md`](../../runner.md) and
[`docs/deployment.md`](../../deployment.md). The current network decision is
recorded in the [current-user network ADR](../../decisions/2026-09-05-windows-current-user-basic-network.md).

The superseded plan covered a dedicated account, WFP, setup, broker/helper IPC,
repair, tamper, and elevated acceptance work. Those tasks are historical
context and have no current operational commands here. Current acceptance is a
separate real Medium run tracked by the Basic evidence ledger.