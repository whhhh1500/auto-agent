# Module optimization ledger — 2026-09-08

## Scope and baseline

This is a research ledger, not an implementation plan or a performance claim.
The original inventory was made at `b752bbe9f38ae1ef69ee5e23824032271a5c3053`
(`feat(server): schedule native model evidence retention`); this review update
was read against `e828a8b141851de17093f2d07c62bd0550dddbc5`
(`fix(execution): reject non-public special-use HTTP destinations`). The
worktree was clean at both checks. The present six-module update was read
against `8e97113a054fb4a38944a8ce4daab0e3c71c014b`
(`feat(rag): reindex CJK delimiter tokens atomically in schema v47`) with a
clean worktree before this batch. The M02/M03 review was read against
`9d1e80f8560d9a25c57136818720f4d024accf08`
(`fix(evaluation): enforce durable dataset and result invariants`) before
concurrent M13 implementation work. The M04-M06 review was read against `17935d2ee8aa7c282774d29a3e0603aeec56708b` (`fix(storage): reject incomplete committed session histories`) with a clean worktree. `go list ./...` on Go 1.25.13 found 90
current build targets. Platform-tagged sandbox helpers and Windows acceptance
commands are recorded below even when they are not targets on the host OS.

The Core boundary remains fixed at 34 production files, 8,817 non-blank lines,
and 910 public API items. This ledger does not authorize a Core or public API
change. All proposed checks use synthetic adapters, local SQL/SQLite fixtures,
or platform acceptance fixtures; none require a billable model provider.

Status meanings:

- **reviewed** — current code and a directly relevant primary source were read.
- **pending** — indexed, but no module-specific source comparison has been
  made in this first pass.
- **not adopted** — a source-informed idea was considered and deliberately
  excluded from the current architecture.

`docs/agent-module-assessment.md` is the stable M01–M44 module index. Package
paths may appear in more than one row where a boundary is intentionally shared.

## Module index

| ID | Current packages and code anchor | Current responsibility | Existing design / test entry | Research status |
| --- | --- | --- | --- | --- |
| M01 | `pkg/core`, `pkg/runtime`; `pkg/core/runtime.go` | Agent loop and runtime event transition contract | Assessment M01; `go test ./pkg/core ./pkg/runtime` | reviewed; offline profile measurement pending |
| M02 | `pkg/core`; `scope*.go`, `principal*.go` | Scope, principal and ownership | Assessment M02; `go test ./pkg/core` | reviewed; ownership/identity separation retained |
| M03 | `pkg/core`; `capability*.go` | Capability registry, manifests and immutable snapshots | Assessment M03; `go test ./pkg/core` | reviewed; snapshot fingerprint semantics retained |
| M04 | `pkg/core`; `profile*.go` | Profile and prompt layering | Assessment M04; `go test ./pkg/core` | reviewed; provider-cache change not adopted |
| M05 | `pkg/core`, `pkg/adapter/coreplugin` | PluginHost compatibility boundary | Assessment M05; `go test ./pkg/core ./pkg/adapter/coreplugin` | reviewed; late-cleanup lifecycle contract gap pending |
| M06 | `pkg/app/capabilityruntime`, `pkg/runtime` | Dynamic capability factory declarations | Assessment M06; `go test ./pkg/app/capabilityruntime ./pkg/runtime` | reviewed; trusted-factory boundary retained |
| M07 | `pkg/storage/{sql_completed_tool_result_fence,sql_native_queued_tool_effect_witness,tool_journal,write_behind}.go`, `pkg/adapter/sql/{effectjournal,fencejournal}` | Module lifecycle, effects and fences | Assessment M07; `go test ./pkg/core ./pkg/storage ./pkg/adapter/sql/effectjournal ./pkg/adapter/sql/fencejournal` | reviewed; generic retry not adopted |
| M08 | `pkg/app/{modelcatalog,modelcontrol}`, `pkg/adapter/modelruntime` | Catalog, plans, plugin selection and compatibility | Assessment M08; `go test ./pkg/app/modelcatalog ./pkg/app/modelcontrol ./pkg/adapter/modelruntime` | pending |
| M09 | `pkg/app/modelexecution/{contract,stream,registry}.go`; `pkg/adapter/modelexecution/{openai,anthropic,corebridge}` | Provider/protocol execution and normalized streaming | Assessment M09; `go test ./pkg/app/modelexecution ./pkg/adapter/modelexecution/...` | reviewed |
| M10 | `pkg/app/{modelsettings,secretview}`, `pkg/adapter/modelsettings`, `pkg/adapter/sql/settings` | Model settings, credential material and cache resolution | Assessment M10; `go test ./pkg/app/modelsettings ./pkg/app/secretview ./pkg/adapter/modelsettings ./pkg/adapter/sql/settings` | reviewed; cache-rotation change not adopted |
| M11 | `pkg/server/server_native_model_checkpoint.go`, `pkg/storage/sql_native_queued_model_*.go` | Model call gate, durable admission and outcome evidence | Assessment M11; `go test ./pkg/server ./pkg/storage` | reviewed |
| M12 | `pkg/runtime`, `pkg/control` | Fast routing and release-aware selection | Assessment M12; `go test ./pkg/runtime ./pkg/control` | pending |
| M13 | `pkg/core/session*.go`, `pkg/storage/sql_session*.go` | Session events and message projection | Assessment M13; `go test ./pkg/core ./pkg/storage` | reviewed; committed-prefix integrity repair implemented and verified |
| M14 | `pkg/app/contextassembly`; `assembler.go`, `context_budget.go` | Context assembly, budgets and recent compression | Assessment M14; `go test ./pkg/app/contextassembly`; implementation/performance context-budget docs | reviewed; estimator candidate is contract-gated |
| M15 | `pkg/core/context_summary.go`, `pkg/app/contextassembly/{extractive_summarizer,summary_transcript,summary_accounting}.go`, `pkg/server/server_summary_accounting*.go` | Summary accounting and long context policy | Assessment M15; `go test ./pkg/app/contextassembly ./pkg/server` | reviewed; retain current safeguards |
| M16 | `pkg/core/policy*.go`, `pkg/server/server_tool_checkpoint.go` | Policy, hooks, schemas, limits and pre-tool protection | Assessment M16; `go test ./pkg/core ./pkg/server` | reviewed |
| M17 | `pkg/storage/approval.go`, `pkg/server/server_run_worker.go`, `pkg/adapter/graphapproval` | Human approval, durable pause and resume | Assessment M17; `go test ./pkg/storage ./pkg/server ./pkg/adapter/graphapproval` | reviewed; broad approval-token change not adopted |
| M18 | `pkg/core/tool_invocation*.go`, `pkg/storage/sql_tool*.go` | Tool journal and unknown-result handling | Assessment M18; `go test ./pkg/core ./pkg/storage` | reviewed |
| M19 | `pkg/storage/{run_control,sql_run_control}.go`, `pkg/server/server_run_worker.go`, `pkg/app/runliveness` | Async queue, leases, workers and liveness | Assessment M19; `go test ./pkg/storage ./pkg/server ./pkg/app/runliveness` | reviewed; jitter and fairness changes not adopted |
| M20 | `pkg/extensions/subagent`, `pkg/storage/delegation_links.go`, `pkg/server/{server_delegation,dynamic_capability*}.go` | Subagent and durable delegation | Assessment M20; `go test ./pkg/extensions/subagent ./pkg/storage ./pkg/server` | reviewed; generic orchestration/retry expansion not adopted |
| M21 | `pkg/extensions/workflow` | Deterministic workflow capability | Assessment M21; `go test ./pkg/extensions/workflow ./pkg/extensions ./pkg/integration` | reviewed; independent workflow checkpointing not adopted |
| M22 | `pkg/app/runexecutor`, `pkg/adapter/runexecutor/graph`, `pkg/execution/graph` | Executor selection and graph execution | Assessment M22; `go test ./pkg/app/runexecutor ./pkg/adapter/runexecutor/graph ./pkg/execution/graph` | reviewed; parallel graph scheduling not adopted |
| M23 | `pkg/extensions/graph`, `pkg/adapter/{memory,sql}/graphcheckpoint`, `pkg/adapter/sql/graphsegment` | Graph checkpoints, history and segment authorization | Assessment M23; `go test ./pkg/extensions/graph ./pkg/adapter/memory/graphcheckpoint ./pkg/adapter/sql/graphcheckpoint ./pkg/adapter/sql/graphsegment` | reviewed; external checkpoint merge/replay not adopted |
| M24 | `pkg/extensions/toollib`, `pkg/execution/mcp_*.go` | Tool library, search and progressive disclosure | Assessment M24; `go test ./pkg/extensions/toollib ./pkg/execution` | reviewed |
| M25 | `pkg/execution/mcp_*.go`, `docs/mcp.md` | MCP process contract, payload limits and observation | Assessment M25; `go test ./pkg/execution` | reviewed |
| M26 | `pkg/execution/{http_executor,ssrf}.go`, `ssrf_special_test.go` | HTTP capability execution and SSRF boundary | Assessment M26; `go test ./pkg/execution` | reviewed; special-use classifier implemented and independently accepted |
| M27 | `pkg/execution/{wazero*,wasm*}.go`, `internal/testwasm` | WASM execution and limits | Assessment M27; `go test ./pkg/execution`; wasm-resource implementation/verification docs | reviewed |
| M28 | `pkg/execution/sandbox`, `pkg/adapter/sandboxexec`, `pkg/adapter/httpapi/sandbox` | Sandbox contracts, registry and admission adapter | Assessment M28; `go test ./pkg/execution/sandbox ./pkg/adapter/sandboxexec ./pkg/adapter/httpapi/sandbox` | reviewed |
| M29 | `pkg/execution/sandbox/windows_*`, `internal/sandbox*`, `cmd/windows-*` | Windows current-user Basic isolation | Assessment M29; platform acceptance commands and Windows-tag tests | reviewed |
| M30 | `pkg/execution/{bwrap_linux,bwrap_other}.go` | Linux local execution and E2B gap | Assessment M30; `go test ./pkg/execution` | pending |
| M31 | `pkg/extensions/runner`, `pkg/adapter/httpapi/runner`, `docs/runner.md` | Private runner and remote worker protocol | Assessment M31; `go test ./pkg/extensions/runner ./pkg/adapter/httpapi/runner` | pending |
| M32 | `pkg/extensions/memory/memory.go`, `pkg/storage/sql_memory_rag.go` | Long-term memory | Assessment M32; `go test ./pkg/extensions/memory ./pkg/storage` | reviewed; retain deterministic bounded recall |
| M33 | `pkg/extensions/rag/rag.go`, `pkg/storage/sql_memory_rag.go` | RAG retrieval | Assessment M33; `go test ./pkg/extensions/rag ./pkg/storage` | reviewed; v47 deterministic lexical reindex accepted |
| M34 | `pkg/storage/sql_native_queued_model_retention.go`, `pkg/storage/sql_schema.go`, `pkg/adapter/sql/sqlkit` | Session persistence, SQL and event queries | Assessment M34; `go test ./pkg/storage ./pkg/adapter/sql/sqlkit` | reviewed; retention sub-batching/index change not adopted |
| M35 | `pkg/storage/{objectstore,s3client,artifact_migration,dynamicstore}.go`, `pkg/app/artifactmigration`, `pkg/adapter/storage/artifactmigration` | Artifacts, object streaming and migration | Assessment M35; `go test ./pkg/app/artifactmigration ./pkg/adapter/sql/artifactmigration ./pkg/adapter/storage/artifactmigration ./pkg/storage` | reviewed; preflight optimization pending measurement |
| M36 | `pkg/app/identity`, `pkg/adapter/httpapi/{auth,accountadmin}`, `pkg/server` | Identity, accounts, auth and admin authorization | Assessment M36; `go test ./pkg/app/identity ./pkg/adapter/httpapi/auth ./pkg/adapter/httpapi/accountadmin ./pkg/server` | reviewed; task-revocation change not adopted |
| M37 | `cmd/server/{settings_bootstrap,storage_config,storage_bootstrap}.go`, `pkg/app/{settings,storageconfig}`, `pkg/adapter/sql/settings`, `pkg/adapter/storageconfig` | Settings, storage config and secret view | Assessment M37; `go test ./pkg/app/settings ./pkg/app/storageconfig ./pkg/adapter/httpapi/settings ./pkg/adapter/httpapi/storage ./pkg/adapter/sql/settings ./pkg/adapter/storageconfig` | reviewed; cache proposal not adopted |
| M38 | `pkg/app/notification`, `pkg/adapter/notification/{coretool,webhook,webhook/targetresolver}`, `pkg/adapter/{httpapi,sql}/notificationtarget` | Notification channels and targets | Assessment M38; `go test ./pkg/app/notification ./pkg/adapter/notification/... ./pkg/adapter/httpapi/notificationtarget ./pkg/adapter/sql/notificationtarget` | reviewed; delivery semantics retained |
| M39 | `pkg/evaluation` | Evaluation datasets and regression gate | Assessment M39; `go test ./pkg/evaluation` | reviewed; Store-invariant repair implemented and accepted |
| M40 | `pkg/control`, `pkg/server/server_telemetry*.go` | Profile release, canary and rollback | Assessment M40; `go test ./pkg/control ./pkg/server` | reviewed; automatic canary mutation not adopted |
| M41 | `pkg/server`, `pkg/adapter/httpapi/{jsonbody,modelsettings,runner,sandbox}`, `openapi/harness-core-v1.yaml` | HTTP API, JSON contract and SSE | Assessment M41; `go test ./pkg/server ./pkg/adapter/httpapi/...`; `go run ./scripts/verify-openapi ...` | pending |
| M42 | `pkg/console`, `pkg/console/static` | Embedded console | Assessment M42; `go test ./pkg/console` | pending |
| M43 | `pkg/telemetry/otel`, `pkg/logging`, `pkg/buildinfo` | Telemetry, logging and build information | Assessment M43; `go test ./pkg/telemetry/otel ./pkg/logging ./pkg/buildinfo` | reviewed; bounded exception event implemented and final gates passed |
| M44 | `internal/modulecheck`, `internal/perfp0`, `cmd/perf-p0`, `scripts/{test-postgres,verify-openapi}` | Tests, architecture guard and performance tooling | Assessment M44; `go test ./internal/modulecheck ./internal/perfp0 ./scripts/...` | reviewed; PostgreSQL evidence lifecycle/O_EXCL implemented and final gates passed |

Additional current build targets not naturally owned by a single M row are
`cmd/{demo,perf-p0,server,starter-server}`, `examples/{crypto,graph-review,runner-worker,starter}`, `internal/{calc,testdb,testwasm}`, `pkg/integration`,
`pkg/buildinfo`, `pkg/logging`, and `scripts`; they are covered by the entry,
operations, testing, and corresponding feature rows above. This prevents the
M01–M44 index from silently excluding the 90-target build graph.

This update leaves **38 modules reviewed and 6 pending**. A reviewed module
can still carry a separately scoped pending measurement or product decision;
that does not make an unproven observation an implementation defect.

## Sources read in this pass

| Source | Date / access | Applied to | Constraint carried into this ledger |
| --- | --- | --- | --- |
| [tau2-bench](https://arxiv.org/html/2506.07982v1), sections 3.2–3.3 | 2025-06; accessed 2026-09-08 | M09, M16, M18, M24–M31, M39 | Model a deterministic task as initialization, allowed solution actions, and final-state assertions. Do not import its domains, simulator, dataset, or reported scores. |
| [AgentDojo](https://arxiv.org/html/2406.13352v3) | 2024-06; accessed 2026-09-08 | M16, M18, M24–M29 | Security evaluation must record both benign task completion and adversarial effect prevention; deny-all is not a safety improvement. Do not import its framework or dataset. |
| [Anthropic: Building effective agents](https://www.anthropic.com/engineering/building-effective-agents) | 2024-12; accessed 2026-09-08 | M09, M20–M25 | Prefer simple composable workflows, observable environment feedback, clear tool interfaces, and stopping conditions; add complexity only with measured benefit. |
| [Anthropic: Writing effective tools for agents](https://www.anthropic.com/engineering/writing-tools-for-agents) | 2025-09-11; accessed 2026-09-08 | M03, M09, M16, M24–M25 | Tool definitions need clear namespaces, strict input/output contracts, and scenario-based evaluation; this does not prescribe a new capability framework. |
| [OpenAI: A practical guide to building agents](https://openai.com/business/guides-and-resources/a-practical-guide-to-building-ai-agents/) | 2025; accessed 2026-09-08 | M16–M18, M21, M24–M31 | Classify tool risk by reversibility, permissions and financial impact; require human intervention for sensitive actions. This supports existing approvals and policy gates, rather than replacing them. |
| [Anthropic: Effective context engineering for AI agents](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents) | 2025-09-29; accessed 2026-09-08 | M01, M04 | Cyclic curation and clear, minimal prompt sections are design comparisons, not proof for this repository's prompt pipeline. |
| [OpenAI Academy: Prompting](https://academy.openai.com/en/public/clubs/work-users-ynjqu/resources/prompting) | published 2025-08-06, updated 2026-09-04; accessed 2026-09-08 | M04 | Clear task/context/output guidance supports inspecting composition; it does not prescribe product prompt text or a Core framework. |
| [Don't Break the Cache](https://arxiv.org/html/2601.06007v2) | arXiv v2, 2026-01-31; accessed 2026-09-08 | M04 | Provider-specific cache experiments require representative measurement; they do not justify reordering governed profile layers or claiming their cost/TTFT results here. |
| [MCP Lifecycle](https://modelcontextprotocol.io/specification/2025-06-18/basic/lifecycle) | MCP specification revision 2025-06-18; accessed 2026-09-08 | M05 | Lifecycle/capability agreement is a comparison for the in-process host; it does not justify importing MCP or alter PluginMount semantics. |
| [Anthropic: Introducing advanced tool use](https://www.anthropic.com/engineering/advanced-tool-use) | 2025-11-24; accessed 2026-09-08 | M05, M06 | Tool declarations do not encode all usage behavior. This is a boundary comparison, not proof that trusted Provides metadata or a factory implementation is safe. |
| [Kubernetes v1.36 manifest-based admission control](https://kubernetes.io/blog/2026/05/04/kubernetes-v1-36-manifest-based-admission-control/) | 2026-05-04; accessed 2026-09-08 | M06 | Only initial rejection of an invalid declaration is comparable; Kubernetes watch/atomic reload is outside this immutable-construction registry. |
| [Anthropic: Scaling managed agents](https://www.anthropic.com/engineering/managed-agents) | 2026-04-08; accessed 2026-09-08 | M01, M13 | Session/harness/sandbox separation is an architectural comparison. It reinforces durable state boundaries but does not validate this repository's behavior or a controlled-deletion repair. |
| [LongRAG](https://arxiv.org/abs/2406.15319) | 2024-06; accessed 2026-09-08 | M14–M15, M33 | Long-context retrieval/chunking results are background evidence only. They do not establish that this repository should change its provider-independent context or summary policy. |
| [A-MEM](https://arxiv.org/abs/2502.12110) | submitted 2025-02; NeurIPS 2025 version accessed 2026-09-08 | M32 | Dynamic graph memory is a research comparison, not a reason to replace the bounded, scope-exact memory contract without a measured harness defect. |
| [RAGChecker](https://arxiv.org/abs/2408.08067) | submitted 2024-08; accessed 2026-09-08 | M33, M39 | Diagnostic retrieval metrics motivate an offline labeled corpus before any retrieval implementation change. |
| [RankRAG](https://arxiv.org/abs/2407.02485) | submitted 2024-07; accessed 2026-09-08 | M33 | Learned ranking performance does not justify adding a model, billing path, or new authority surface to deterministic scoped retrieval. |
| [Temporal Workflow Execution](https://docs.temporal.io/workflow-execution) | current documentation, copyright 2026; accessed 2026-09-08 | M07, M11, M18 | Event-history-checked replay is a useful recovery principle; it does not authorize provider/tool replay outside this repository's exact evidence contracts. |
| [AWS Well-Architected REL05-BP03](https://docs.aws.amazon.com/wellarchitected/2023-04-10/framework/rel_mitigate_interaction_failure_limit_retries.html) | updated 2023-07; accessed 2026-09-08 | M18–M19 | Retry only operations known to be retryable and idempotent. This is a foundation, not 2024–2026 frontier research. |
| [AWS: Exponential Backoff and Jitter](https://aws.amazon.com/blogs/architecture/exponential-backoff-and-jitter/) | published 2015-03, updated 2023-05; accessed 2026-09-08 | M19 | Jitter is a comparison baseline only. It is foundational material, not frontier research, and the local default-parameter experiment rejects adoption. |
| [Aequitas admission control](https://research.google/pubs/aequitas-admission-control-for-latency-critical-rpcs-in-datacenters/) | SIGCOMM 2022; accessed 2026-09-08 | M19 | Fairness requires scheduling state and goals outside the present queue contract. It is a comparison, not a proposal to claim tenant fairness. |
| [PostgreSQL: Routine Vacuuming](https://www.postgresql.org/docs/current/routine-vacuuming.html) | current PostgreSQL 18 documentation; accessed 2026-09-08 | M34 | `DELETE` creates MVCC dead rows and fixed maintenance schedules have I/O trade-offs. This is current official documentation, not a research paper. |
| [Anthropic: Demystifying evals for AI agents](https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents) | 2026-01-09; accessed 2026-09-08 | M33, M39, M44 | Separate diagnostic capability-quality measurements from near-perfect regression checks; both may examine final environment state and transcripts, with a code grader chosen only where its limits fit the assertion. M39's Store repair is implemented and cross-store accepted; M44's runner evidence path is reviewed. |
| [Anthropic: Quantifying infrastructure noise](https://www.anthropic.com/engineering/infrastructure-noise) | 2026-02-05; accessed 2026-09-08 | M44 | Benchmark configuration can shift measured results. Record and control that environment before changing metrics or inferring a performance regression; this is not evidence of an observed instability here. |
| [SSRF vs. Developers: A Study of SSRF-Defenses in PHP Applications](https://www.ias.tu-bs.de/publications/ssrf-usenix-2024.pdf) | USENIX Security 2024; accessed 2026-09-08 | M26, M38 | Use its input-to-parse-to-DNS-to-dial-to-redirect-to-response taxonomy to audit this Go data flow. It is a PHP study: do not extrapolate its measured vulnerability rates to this repository. |
| [AutoGPT advisory GHSA-wvjg-9879-3m7w](https://github.com/Significant-Gravitas/AutoGPT/security/advisories/GHSA-wvjg-9879-3m7w) | 2025-04-11; accessed 2026-09-08 | M26 | A concrete agent-system case involving manual redirects, cross-origin secret exposure and DNS. It motivates redirect/credential regression cases; it is not evidence of the same defect here. |
| [IETF HTTP Idempotency-Key Header Field](https://datatracker.ietf.org/doc/draft-ietf-httpapi-idempotency-key-header/) | current IETF draft; accessed 2026-09-08 | M26, M38 | Idempotency is a receiver/server contract, not a client-side exactly-once guarantee. It supports retaining the existing stable key and explicit unknown-result boundary. |
| [RFC 9421: HTTP Message Signatures](https://www.rfc-editor.org/rfc/rfc9421) | 2024; accessed 2026-09-08 | M38 | Standardized signing requires an interoperable receiver verification and replay contract; it is not a drop-in replacement for the current configured HMAC timestamp/body convention. |
| [Go `net/http` documentation](https://pkg.go.dev/net/http) | current official API documentation; accessed 2026-09-08 | M26, M38 | `CheckRedirect`, `Transport.DialContext`, request context propagation and response-body lifetime are the concrete library contracts behind the reviewed transport policies. |
| [Amazon S3 default data-integrity protections](https://aws.amazon.com/about-aws/whats-new/2024/12/amazon-s3-default-data-integrity-protections/) and [multipart checksum protocol](https://docs.aws.amazon.com/AmazonS3/latest/userguide/mpuoverview.html) | 2024-12 release and current official protocol; accessed 2026-09-08 | M35 | Checksums support current digest reconciliation; they do not justify removing commit-time CAS or adding an unmeasured preflight. |
| [Google Cloud Storage Object Retention Lock](https://cloud.google.com/blog/products/storage-data-transfer/introducing-cloud-storage-object-retention-lock) | 2024-04; accessed 2026-09-08 | M35 | Provider object lock conflicts with the portable overwrite/delete/migration-cleanup contract. |
| [AWS Secrets Manager transform update](https://aws.amazon.com/blogs/security/introducing-an-enhanced-version-of-the-aws-secrets-manager-transform-awssecretsmanager-2024-09-16/) and [Vault cache limits](https://developer.hashicorp.com/vault/docs/agent-and-proxy/proxy/caching) | 2024-12 and current official documentation; accessed 2026-09-08 | M37 | A cache can serve stale or revoked material; no TTL cache should be added without revision and permission-revocation invalidation. |
| [Anthropic: How we built our multi-agent research system](https://www.anthropic.com/engineering/multi-agent-research-system) | 2025-06-13; accessed 2026-09-08 | M20 | An orchestrator-worker system needs concrete subtask boundaries, effort limits and observability; its parallel research architecture is not a reason to add a generic worker pool or automatic child replay here. |
| [Anthropic: Effective harnesses for long-running agents](https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents) | 2025-11-26; accessed 2026-09-08 | M13, M22, M23 | Long-running agents need durable handoff state across context windows. This motivates checking committed session history and reviewing frozen definitions/recovery boundaries; it does not prove a storage repair or justify a foreign checkpoint runtime. |
| [LangGraph Persistence](https://docs.langchain.com/oss/javascript/langgraph/persistence) | current official technical documentation; accessed 2026-09-08 | M17, M22, M23 | Persisted checkpoints and pending writes are a direct comparison for pause/resume. They do not establish that this repository can replay an executing node with an unknown external effect. |
| [Unicode UAX #29, revision 47](https://www.unicode.org/reports/tr29/tr29-47.html) | Unicode 17.0, 2025-08-17; accessed 2026-09-08 | M33 | Reliable Chinese/Japanese word boundaries need dictionaries or other mechanisms. It limits the v47 candidate to explicit punctuation delimiters; it is not an implementation of UAX #29 or Chinese segmentation. |
| [Google Secret Manager best practices](https://docs.cloud.google.com/secret-manager/docs/best-practices) and [rotation recommendations](https://docs.cloud.google.com/secret-manager/docs/rotation-recommendations) | best practices updated 2026-08-26 and current official rotation guidance; accessed 2026-09-08 | M10 | Rotation and disablement need an explicit version/rollout contract. They do not make a deployment-global five-second cache an authorization bug or supply cross-instance invalidation. |
| [RFC 9700: Best Current Practice for OAuth 2.0 Security](https://www.rfc-editor.org/rfc/rfc9700.html) | IETF BCP 240, 2025-01; accessed 2026-09-08 | M02, M36 | Resource/action least privilege and per-request audience checks are useful boundary comparisons. This repository does not implement OAuth; the RFC is not a protocol adoption proposal. |
| [MCP Authorization specification](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization) | MCP specification, 2025-11-25; accessed 2026-09-08 | M03 | HTTP transport token audience binding belongs to MCP/OAuth resource-server handling. It does not make a local capability snapshot ID a credential or authorization-token format. |
| [OpenTelemetry CI/CD Observability](https://opentelemetry.io/blog/2025/otel-cicd-sig/) | OpenTelemetry engineering article, 2025-02-24; accessed 2026-09-08 | M40, M43 | Standard release and VCS correlation can improve observation, but does not establish an automatic promotion or rollback policy for this repository. |
| [OpenTelemetry exceptions](https://opentelemetry.io/docs/specs/otel/trace/exceptions/) and [Tracing API](https://opentelemetry.io/docs/specs/otel/trace/api/) | current OTel specifications; accessed 2026-09-08 | M43 | An unhandled span failure is represented by a standard `exception` event and error span status. This supports preserving the standard event shape while locally bounding its message. |
| [Go test packages](https://pkg.go.dev/cmd/go#hdr-Test_packages) | current Go command documentation; accessed 2026-09-08 | M44 | The Go test JSON stream distinguishes package events from named test/subtest lifecycle events; runner evidence needs a complete named-test lifecycle rather than any terminal event. |

## Reviewed-module source mapping and candidate status

This section is deliberately more specific than the module table: every
`reviewed` row names the source, current code evidence, and the implementation
status. A reviewed source comparison is not an approval to change code.

| Module | Current code evidence | Source correspondence | Candidate and status | Explicit boundary |
| --- | --- | --- | --- | --- |
| M01 runtime and agent loop | `runtime.go:59-294` resolves current profile, capabilities, model and policy around `RunTurn`, `ResumeTurn` and post-result `ContinueTurn`, while leaving transport and persistence outside Runtime. `agent.go:17-21,153-349,406-425,540-567` serializes a run, enforces steps/tool budgets and appends durable events. `agent_model_call.go:14-94` records model-gate/context evidence; `model_context.go:16-81` clones and validates assembler output. Existing model-context, loop, recent-compaction and benchmark tests cover these contracts. | Anthropic's 2025 context-engineering article supports cyclic curation as a design concern; its 2026 managed-agents article is a comparison for session/harness/sandbox separation. Neither supplies a repository-specific performance or correctness result. | **reviewed; no framework change adopted. pending measurement:** an offline long-history, real-`ContextAssembler` profile scenario should record existing context bytes/tokens/dropped groups and assert the latest turn/tool dependencies remain complete. | Do not import an agent framework, add a reset protocol, change Core/API budgets, or call current unit tests and benchmarks a profile measurement. The candidate uses existing targeted core tests and benchmark seams without a provider call. |
| M02 scope, principal and ownership | `scope.go:40-108` validates an ordered immutable ownership path and ancestor tests. `session.go:250-278` requires a nonempty subject/tenant and an owning principal scope for a session. `model_gate.go:181-211` and `trust_gate.go:57-61` compare subject, tenant, scope, grants and attributes exactly before an accepted model/tool invocation proceeds. Existing tests cover delimiter rejection, session identity/scope construction, different-scope rejection, bounded claims and unforgeable accepted calls. | RFC 9700 requires resource/action least privilege and per-request resource-audience verification. That maps to the repository's ownership/permission and full-principal gates, not to parsing OAuth tokens in Core. | **reviewed; no confirmed isolation defect. pending ingress audit:** `ScopePath` rejects `/` and NUL but not every Unicode control character. No untrusted ingress, key collision or authorization bypass was found, so this is not a repair candidate yet. | Do not require `TenantID` or `SubjectID` to equal a `tenant`/`user` path segment: platform principals may legitimately carry business identity while owning global or product scopes. Do not add an OAuth token model or change Core/API scope semantics. |
| M03 capability registry, manifest and snapshot | `capability.go:309-348` verifies ownership, applies ordered bindings and permission filtering, then clones manifest definitions into a fixed selection. `profile.go:540-575` only subtracts capabilities and rebuilds a new snapshot. `capability.go:430-462,533-649` keeps principal/manifests defensive and public views redacted; `runtime.go:239-246,307-315` persists the public definitions and effective permissions while excluding literal header values. Existing tests cover frozen layers, protected overrides, permission filtering, provider revisions, defensive copies, redaction and provider non-exposure. | Anthropic's 2025 tool guidance supports clear namespaced tool contracts and scenario evaluation. MCP's 2025 authorization specification requires audience validation for HTTP access tokens, a different transport concern. | **reviewed; no confirmed snapshot defect.** `snapshotDigest` intentionally fingerprints target scope, subject and effective capability definitions, not every principal claim or credential. This batch documents that semantic at `README.md:178`; no production implementation is proposed. | Snapshot ID is a frozen capability-selection fingerprint, not a full-principal, credential, or token authorization proof. Model/trust gates still compare the full principal. Do not add MCP/OAuth fields or expand the digest without an explicit stable-ID and audit-consumer contract. |
| M04 profile and prompt layering | profile.go:16-32,340-395,434-530 applies layers from ancestor to target scope; scalar overrides and ID-based capability/fragment changes resolve to a bounded snapshot sorted by section, priority and ID. runtime.go:180-246 resolves it with the current principal, supplies SystemPrompt, and persists an audit composition; agent.go:206-210,392-400 writes start/resume evidence. profile_test.go:8-58 covers inheritance/replacement and profile_replace_test.go:20-180 covers precedence, atomic resolve and detached layers. | Anthropic supports clear, minimal sections; OpenAI Academy treats prompt form as iterative task/context/output design. The cache paper requires provider/workload measurement. | **reviewed; KEEP. not adopted:** cache-driven layer reordering, profile cache framework, protected-fragment semantics, or keyword prompt redaction. Any adapter-owned cache experiment needs a named provider cache-token contract plus same-profile cost/TTFT data; normalized usage has only input/output tokens. | Layer order, current-principal composition, epoch publication and resume evidence are correctness contracts, not cache layout hints. Prompt fragments are deliberate model/audit content; do not mutate prompts or evidence with generic redaction. |
| M05 PluginHost | plugin.go:18-39 is a scope-fixed in-process host. OnUnmount at :125-135 has no closed check; MountedPlugin.Close at :173-187 marks closed then snapshots callbacks. A Go 1.25.13 isolated repro at D:\cc\auto_agent\.codex-v46-audit\m05-pluginhost\late-onunmount-20260908\late-onunmount-repro.{log,exit.txt} reports early_cleanup=1 late_cleanup=0; current tests register only synchronously during Install. adapter/coreplugin/adapter.go:30-47,75-84,236-242 keeps declared-provides compatibility trusted, rejects effects, validates mounted ID/version and fails recovery closed. | MCP Lifecycle is only a lifecycle comparison. Anthropic's tool-use article distinguishes declarations from usage but does not establish that this repository's trusted Provides metadata is safe or complete. | **reviewed; pending API-semantics repair.** A late callback is silently lost. The current comment requires synchronous Install-only registration; this clarifies, but does not fix, the public lifecycle gap. No production asynchronous registration call was found. **not adopted:** declaration-to-registration bijection; layered duplicates and compatibility semantics need new metadata/contracts. | Do not call the comment-only change a repair, import MCP, turn PluginHost into a Go .so loader, or expand Core/API merely to enforce declaration completeness. |
| M06 dynamic capability factories | capabilityruntime/registry.go:1-205 accepts only trusted bounded ID/version factories, contains identity/revision/New panics, and clones request/result data. server_dynamic_capability.go:105-168,199-280 validates a binding before publication, records implementation revision, and in epoch-admission mode accepts only package-private server-owned factories; built-ins validate HTTP/runner/subagent configuration. server_restore.go:135-163 rechecks runtime ID/version and implementation revision, failing closed on mismatch. registry_test.go:39-156 covers version/not-found, capacity, panic, cancellation and deep-copy failure; runtime/host_apply.go:9-141 separately validates staged manifests before atomic publication. | Kubernetes compares only initial invalid-declaration rejection; its watched reload is not this registry's immutable construction model. Anthropic's tool-use article supports distinguishing declared schema from runtime behavior without making a database declaration executable code. | **reviewed; KEEP.** No binding can load an unknown factory; strict epoch mode rejects third-party construction before publication. **not adopted:** source upload, factory discovery/watch loop, or a generic factory-policy framework. | A manifest declaration is not provider implementation proof. Keep factory code trusted and composition-owned; do not infer permanent immutability from the Kubernetes comparison or weaken strict construction rules. |
| M09 model adapters | `pkg/app/modelexecution/stream.go` rejects post-finish events, duplicate usage, conflicting tool fragments, oversized arguments and invalid finish/tool combinations; `registry.go` applies it before emitting normalized events. | tau2-bench's init/solution/assertion pattern and Anthropic's tool-interface guidance support scenario-level contract tests. | R-01, **pending design review**: synthetic cross-adapter normalized-stream scenarios. | No provider call, no new adapter framework, and no production behavior change. |
| M11 durable model outcomes | `pkg/server/server_native_model_checkpoint.go` and `pkg/storage/sql_native_queued_model_*.go` fence admission and persist outcome evidence. | tau2-bench supports final durable-state assertions; it is a test-design input only. | **pending measurement**: include durable admission/outcome/retry state in existing deterministic recovery cases if an uncovered sequence is identified. | Do not import the benchmark, change recovery protocol, or consume Core/API budget. |
| M14 context assembly | `assembler.go:40` injects `ContextEstimator`; `assembler.go:68` combines context window, max output and safety margin; `context_budget.go:171,178,358` keeps tool call/result groups atomic and fails a required-group budget closed. `ConservativeEstimator` at `assembler.go:199` intentionally uses an upper-bound UTF-8-byte model. | LongRAG is only background. It does not prove a provider-independent tokenizer estimate or a change to the current framing contract. | **pending provider-contract research**: an app-layer estimator for one named protocol may be evaluated only after that provider's serialized-input/framing contract establishes a conservative upper bound. | Golden CJK/emoji/tool-schema tests alone cannot prove `estimate >= actual` for every provider protocol or billing rule. Keep the byte upper bound as default; no Core tokenizer dependency. |
| M15 summary and long context | `pkg/core/context_summary.go` exposes only the `RunSummarizer` contract; `extractive_summarizer.go:71` is one-pass and bounded; `summary_transcript.go:30` bounds LLM transcript input. Tool call/result pairing and summary accounting have focused tests. | LongRAG reports long-context retrieval/chunk choices, not a correctness proof for summary replacement. | **not adopted**: no provider-summary switch, expanded transcript, or altered tool-pair rule is proposed. Current safeguards remain the reviewed choice. | Preserve bounded, deterministic extractive fallback and usage accounting. |
| M16 policy and pre-tool protection | `pkg/core/policy*.go` resolves capability/policy; `pkg/server/server_tool_checkpoint.go` records effect admission before non-approval effects. | AgentDojo requires utility and attack resistance to be evaluated together; OpenAI's guide supports proportionate approval for sensitive actions. | R-02, **pending design review**: paired benign/adversarial deterministic tool-output contracts. | Do not add an LLM classifier or make the policy path depend on billable inference. |
| M18 tool journal | `pkg/core/tool_invocation*.go` and `pkg/storage/sql_tool*.go` retain invocation state and unknown-result evidence. | tau2-bench supports final-state assertions; AgentDojo supports paired safety/utility cases. | R-02, **pending design review**: journal effects become the durable assertion surface for local injection fixtures. | No fake success result and no change to effect/fence semantics. |
| M24 tool library | `pkg/extensions/toollib` supplies discovery and progressive disclosure over the capability surface. | Anthropic's tool-interface guidance and AgentDojo's evaluation shape. | R-02, **pending design review**: safe/benign fixture pairs using the existing discovery surface. | Do not import AgentDojo tool domains/framework or modify tool-library runtime behavior. |
| M25 MCP | `pkg/execution/mcp_*.go` bounds MCP payload and process behavior. | Anthropic's tool-interface guidance and AgentDojo's paired-evaluation shape. | R-02, **pending design review**: optional local MCP payload fixture under existing limits. | Preserve MCP limits and process contracts; no new MCP framework. |
| M27 WASM | `pkg/execution/{wazero*,wasm*}.go` and `internal/testwasm` enforce local execution/resource behavior. | AgentDojo motivates adversarial tool-output cases, not a new WASM execution policy. | R-02 has optional synthetic capability cases, **pending design review**. | No resource-limit relaxation or model-derived execution decision. |
| M28 sandbox contracts | `pkg/execution/sandbox`, `pkg/adapter/sandboxexec`, and `pkg/adapter/httpapi/sandbox` enforce assurance, network and resource declarations. | AgentDojo's test shape supports checking enforcement without equating it with model robustness. | R-02 has optional synthetic network-denial and approval-required cases, **pending design review**. | No assurance downgrade, host-network equivalence claim, or generic fallback. |
| M29 Windows Basic sandbox | `pkg/execution/sandbox/windows_*`, `internal/sandbox*` and `cmd/windows-*` contain platform-specific current-user isolation evidence. | AgentDojo does not establish a Windows isolation claim; it only motivates adversarial input coverage. | **not adopted**: no general sandbox integration follows from this source comparison. | Platform acceptance remains the only basis for a Windows assurance claim. |
| M32 long-term memory | `pkg/extensions/memory/memory.go:30` caps scope entries at 1024 and `:184` performs exact-scope recall; `pkg/storage/sql_memory_rag.go:222-257` uses lower-case substring/tags and stable recency/id ordering. | A-MEM's dynamic memory graph is a useful contrast, but it changes the repository's bounded deterministic scope/upsert contract. | **not adopted**: do not add graph memory absent a measured recall defect and explicit new contract. | Keep exact scope, bounded entries and deterministic ordering; do not claim semantic relevance ranking exists today. |
| M33 RAG retrieval | `pkg/extensions/rag/rag.go:113-122` tokenizes deterministically; `:228` exposes the reference ranker. `pkg/storage/sql_memory_rag.go:381-481` applies scope prefixes/default top-K and stable unique-overlap ranking; parity is covered in `pkg/storage/infra_test.go:309,343`. Test-only baseline is `pkg/storage/sql_rag_quality_baseline_test.go` with `pkg/storage/testdata/rag_quality_baseline.json`. | RAGChecker motivates diagnostics; RankRAG demonstrates learned ranking, which is unsuitable without a measured gap and authority/cost review. Anthropic's 2026 evaluation guidance distinguishes this diagnostic quality corpus from an isolated near-perfect regression gate. | **small fixed regression baseline; diagnostic measurements, not a production-quality threshold or improvement**. SQLite and a real PostgreSQL rerun of `TestPostgresSQLRagQualityBaseline` both passed (native exit 0) with micro Recall@5 `5/7`, micro precision@5 `5/10`, macro MRR@5 `0.500`, and expected misses `semantic-billing-date`, `cjk-receipt`. Recall is relevant returned / all qrels, precision is relevant returned / all returned top-five chunks, and macro MRR assigns zero when no relevant result is retrieved. The hand-authored qrels exercise inheritance/sibling isolation, any-tag behavior, a long lexical distractor, semantic miss, and CJK punctuation miss; they are not representative of production traffic. The first PostgreSQL attempt found missing `rag_documents` schema setup in the test harness; the harness initialization was corrected and the real rerun passed. This was not a production RAG defect. A private deterministic lexical second stage remains **pending** and needs a separately measured acceptance criterion. | Do not add an LLM reranker, dense index, fusion framework, scope/tag leakage or cross-database nondeterminism. |
| M33 Unicode-token reindex | Repo-external design/evidence is `D:\cc\auto_agent\audit\rag-unicode-tokenization-20260908\v47-unicode-delimiter-design.md`; the implementation uses only `，、。；：！？（）［］【】「」『』《》〈〉` as delimiters and preserves curly quotes, dashes, ASCII URL/email/code and `l’été`. UAX #29 revision 47 says reliable Chinese/Japanese word boundaries need dictionaries or other mechanisms, so this is deliberately a fixed punctuation-delimiter reindex, not UAX #29 conformance or Chinese segmentation. | The actual fixed qrels now measure only the intended lexical change: micro Recall@5 `6/7`, micro precision@5 `6/11`, macro MRR@5 `0.700`, with `semantic-billing-date` still the sole diagnostic miss. These are a small fixed regression baseline and diagnostic measurements, not a production-quality threshold or a paper score. | **implemented and independently accepted.** Schema v47 atomically rebuilds the persisted projection and marker; SQLite and clean single-writer strict PostgreSQL acceptance passed. The strict-PG JSONL evidence has 1,434/1,434 parseable lines, 179 named runs passed, 104 top-level tests, 75 subtests, 10 packages, and zero named skips/failures. An e828a8b-built opener accepts an independent v46 fixture, rejects schema 47 before mutation, and preserves the v47 fixture SHA-256; this protects against an old binary opening a migrated database, but cannot stop an already-running legacy writer. | This is not general Chinese segmentation or a claim about production traffic. Do not add a dense/LLM ranker, alter scope/tag authorization, or infer protection against concurrent legacy writers from the open-time version check. |
| M17 approval pause/resume | `storage/approval.go:129-208` derives a stable request from tenant/subject/session/run/call/manifest facts; `:357-404` decides once, changes only a waiting non-cancelled run, and wakes its durable queue row in one transaction. The queued worker re-resolves the current principal before runtime (`server_run_worker.go:398-413`). Graph resumes additionally bind approval ID, checkpoint revision, source segment and a newer host generation (`adapter/graphapproval/adapter.go:71-124`; `execution/graph/executor.go:967-989`). | OpenAI's 2025 guide supports intentional human intervention for sensitive action. LangGraph persistence is a useful pause/resume comparison, but its generic pending-write model is not proof for this repository's authorization or effect history. | **reviewed; no confirmed authorization-epoch defect.** A test-only linearization matrix remains **pending measurement**: decision vs cancellation/expiry and stale graph revision/segment must leave exactly one recorded outcome and no continuation. | Do not add a broadly reusable approval token or treat an old decision as perpetual authority. SQL queue and Graph deliberately re-establish different current facts before continuation. |
| M20 durable delegation | `subagent.go:342-431` deterministically derives one child session/run from the parent guarded invocation, persists the link with `PutIfAbsent`, validates tenant/subject/depth, and returns terminal results or resumes the same approval-waiting child. `storage/delegation_links.go:14-140` stores metadata only and treats a different child conflict as non-idempotent. `childApprovalPending` at `subagent.go:434-455` suspends the parent rather than letting it orphan a nested approval; `server_delegation.go:17-61` requests direct-child cancellation and closes waiting child approvals when a parent is cancelled. Existing tests cover restart replay, concurrent replay, response-lost convergence and retryable context cancellation. | Anthropic's 2025 multi-agent engineering report requires explicit objectives, boundaries and effort limits, and warns that coordination cost rises quickly. It motivates inspecting delegation boundaries; it does not prove that parallel child execution or automatic retry is safe. | **reviewed; no confirmed completion/orphan defect. not adopted:** a generic orchestrator pool, automatic re-run of nonterminal children, or cross-parent join. **pending measurement:** one local parent-cancel-after-child-create fixture can assert control cancellation, child approval closure and no second child run. | Preserve one stable child per parent call, depth/budget/ownership checks and fail-closed unknown child state. Do not turn a missing session or response ambiguity into a new child side effect. |
| M21 sequential workflow | `workflow.go:37-77` freezes a bounded definition and cloned JSON arguments. `:103-156` resolves each step only through the active run's protected invoker, derives stable child call IDs, propagates `ApprovalPendingError`, and stops at the first unsuccessful step. Tests cover invoker absence, immutable definitions, protected tool budget, references and durable approval integration. | Anthropic's 2024 workflow guidance favors simple sequential composition where subtasks are fixed, with environment feedback and stopping conditions. OpenAI's 2025 guide supports intentional approval points. Neither source makes an independently replayable workflow step safe. | **reviewed; no confirmed deterministic-workflow defect. not adopted:** a workflow-local checkpoint/retry layer. **pending measurement:** a synthetic idempotent-step matrix may confirm that an approval pause resumes through the existing protected invoker without duplicating the earlier step. | The surrounding run, tool journal and approval contracts own durability. A local retry could bypass admission/effect fencing or duplicate a non-idempotent tool. |
| M22 graph executor | `adapter/runexecutor/graph/graph.go:125-243` permits only the built-in definition, acquires a segment grant for each turn/resume, and routes approvals through the resolver. `execution/graph/executor.go:197-329` creates/continues matching ready checkpoints but converts abandoned `executing` work to `unknown` under a new segment/generation without invoking the interrupted node. `:395-515` checks the lease before and after node code, retries only explicit retryable failures under bounded policy, and leaves a possibly side-effecting stale execution uninterpreted. | Anthropic's 2025 long-running-harness article motivates durable handoff across context windows; LangGraph persistence is a comparison for stored state. Neither establishes equivalence between a generic graph executor's pending writes and this repository's unknown external-effect boundary. | **reviewed; no confirmed graph-executor defect. not adopted:** parallel scheduling, broad execution replay or generic recovery of `executing`. **pending measurement:** deterministic fault injection can classify node outcomes as pre-effect retryable versus post-effect unknown, with no provider/tool call. | Keep the built-in sequential executor, bounded retries and post-node lease check. Unknown effects must stay terminal/visible until a separately proven recovery protocol exists. |
| M23 graph checkpoints and segment authority | `extensions/graph/checkpoint.go:94-178` binds frozen definition/composition/implementation revisions, segment/host generation, deterministic transition identity, approval evidence and an atomic checkpoint-plus-transition CAS contract. The SQL store writes head, immutable version and transition transactionally; its exact-replay check requires byte-equivalent state (`adapter/sql/graphcheckpoint/store.go:253-404`). `graphsegment/store.go:48-177` grants a fresh segment/holder/generation and verifies it on every mutation. Validation and tests reject stale approval evidence, CAS conflicts, weak history fences and stale leases. | LangGraph checkpoint documentation describes persisted snapshots and pending writes; Anthropic's long-running-harness article establishes the need to carry durable state between context windows. These sources are useful comparisons, not proof that foreign checkpoint history can be merged or that failed nodes may be replayed. | **reviewed; no confirmed checkpoint/history defect. not adopted:** external checkpoint merge, automatic fork execution, or cross-runtime replay. **pending measurement:** retain the current SQLite/PostgreSQL CAS/history/lease matrix and add only a local assertion that a fork-origin version cannot be selected as an active head without an explicit future contract. | Do not change the narrow Store port, schema, Core or public API. Checkpoint history is evidence, while current segment authority and frozen revisions decide whether execution may continue. |
| M07 lifecycle/effects/fences | `sql_completed_tool_result_fence.go`, `sql_native_queued_tool_effect_witness.go:81` and `tool_journal.go` lock and revalidate queue/session/lease generation. `write_behind.go:17-31,185-234` retains the first append failure terminally. | Temporal replay checks commands against history, but v43/v44 plus journal and fenced append form this repository's stricter per-effect proof. | **not adopted**: no generic `WriteBehind` retry. It could obscure an atomic v45/v46 prefix/outcome failure. | No schema/Core/API change; failure stays terminal rather than becoming an implicit provider/tool retry. |
| M11 durable model outcomes, recovery detail | `sql_native_queued_model_invocation.go:66-72` records v45 admission; `sql_native_queued_model_outcome.go:45-65` writes atomic v46 plus suffix; `:316-411` derives a digest from admission or a legal persisted chunk boundary, never an arbitrary intra-chunk suffix. | Temporal supports history-bound recovery but does not justify model provider replay. | **not adopted**: generic auto-resume/retry; unknown v45 remains fail-closed. | Keep sequential, current-authority re-admission and exact identity proof. |
| M13 session projection integrity | A controlled local repro had saved two events, deleted raw SQL `event_chunks` while retaining version `2`, and observed `Load` return version `0` with zero events. This batch adds one shared `0..core.MaxSessionEvents` committed-version guard to ordinary SQL restore, fenced SQL restore, S3 restore, and S3 canonical evidence rebuild. SQL/fenced/S3 restore now reject a short restored prefix while retaining the pre-existing behavior that trims an overlong uncommitted suffix. | Anthropic's long-running-harness and managed-agent articles provide durability design context, not validation of this repair. The repro was controlled raw deletion, not evidence of autonomous production corruption. | **implemented and verified.** SQLite/PostgreSQL ordinary and fenced negative paths plus empty-tail cases pass. Clean strict PostgreSQL evidence is native 0 with 1,550 parseable JSONL lines, 107 top-level tests, 101 subtests, 10 packages, and zero named skips/failures or lifecycle anomalies. The relevant non-PG race scope (all SQL/S3 session-store tests plus SQLite fenced acquire/interrupted repair/write-behind/completed-tool state machines) is native 0 in 229.759 s. | This is not a full-repository race claim and does not auto-repair history. Loads fail closed on invalid versions or missing committed evidence; execution ownership still decides whether explicit crash-tail repair may append events. |
| M18 tool journal, recovery detail | `tool_journal.go` and `sql_native_queued_completed_tool_recovery.go:29-152` perform A atomic delivery or B exact sidecar readback, each with current epoch/fence and v44/journal proof. | Temporal history and AWS's retry-only-when-idempotent principle. | **not adopted**: recovery for partial streams, multiple pending tools, or generic V3-only history. | Unknown results must never provoke an external tool/provider replay. |
| M19 queue, leases and liveness | `run_control.go:159-190` implements FIFO plus PostgreSQL `SKIP LOCKED`; `:487-549` increments attempt/generation; `:602-636` fences retry. `server_run_worker.go:232-293` starts empty polling at 250 ms and backs idle polling off to 1 s; `:631-678` uses fixed exponential retry. Defaults are `DefaultRunMaxAttempts=3` at `run_control.go:24`. | AWS jitter is foundational comparison material; Aequitas shows that fairness requires explicit scheduling state and policy. Neither is a claim of current frontier evidence. | **rejected after local experiment**: equal jitter. The fixed-seed, aligned-poll model at the default 250 ms interval leaves 1,000 workers in one observed bucket on attempt 1 for both fixed and equal jitter; on attempt 2, fixed/equal maximum buckets are 1,000/995. With default max attempts 3, only attempts 1–2 retry. **not adopted**: cross-tenant fairness, which requires durable virtual-time/deficit/weight state absent from the contract. | The model uses seed `20260908`, integer-ms due times and common poll phase; it excludes SQL contention, claim service, leases, provider calls and throughput. A lower simulated mean scheduled delay is not a throughput improvement. The real empty-idle poll can back off to 1 s, which this 250 ms aligned upper-bound check does not model. Raw script/artifact remain outside the repository. |
| M34 SQL/session retention | `sql_native_queued_model_retention.go:14-82` bounds candidates to 8192 and uses one transaction; `:85-181` locks/rebuilds exact history, validates v45/v46 plus digest, and deletes only that pair. `server_native_queued_retention.go:21-46` enables it only on a sealed Native-static worker loop. v43/v44/journal and v45 without v46 remain intact. | PostgreSQL vacuuming documentation explains the MVCC cost after deletion, without prescribing this application transaction shape. | **not adopted this round**: retention sub-batching or an additional created-at index. Both alter partial-progress/error semantics or schema, beyond scope. | Preserve bounded loop and proof-based pruning; no generic retention behavior change. |
| M26 HTTP/SSRF | `HTTPExecutor.Execute` validates the bind-time URL, refuses a custom client unless `AllowPrivateNetwork` is explicit, bounds headers/query/request body/response body, and propagates the call context. `newPublicHTTPClient` sets no proxy and `CheckRedirect` refuses redirects; its `DialContext` resolves immediately before connecting and rejects non-public answers. The private IANA classifier now rejects special-use non-global ranges, preserves explicit global exceptions, and tests literal bind plus DNS public-at-bind/special-at-dial rechecks with no real connection. | The SSRF USENIX paper supplies the input/parse/DNS/dial/redirect/response audit taxonomy; the AutoGPT advisory supplies a directly relevant manual-redirect/secret-leak threat case. The IANA registry makes public-only classification precise without treating N/A transition ranges as globally reachable. | **implemented and independently accepted**: `go test ./pkg/execution -count=1 -v`, `go vet ./pkg/execution`, and targeted SSRF/HTTP `-race` all returned native 0. The package run has one existing Wazero cancellation-subprocess guard skip; it is not claimed as zero-skip. **pending characterization**: a local 4xx echo server can demonstrate that generic HTTP returns a truncated non-2xx body to the capability result; decide whether a credential-echo redaction rule is required before adding a failing security test. | This is not a claim of a demonstrated external SSRF exploit. Default connection-time validation, redirect refusal and proxy disablement remain in force. Do not redact generic protocol errors without a product compatibility decision. `AllowPrivateNetwork` intentionally transfers egress assurance to the deployer-controlled client/network boundary. |
| M38 notifications/webhooks | `notification.Delivery` is bounded and non-secret; target refs are opaque. SQL target storage is tenant-scoped, encrypts private configuration and omits it from list projections. `targetresolver` accepts only strict `{url,secret}` configuration and clears owned buffers. `webhook.Channel` accepts HTTPS, no userinfo/fragment/secret query, resolves each connection to public IPs, disables proxy and redirects, bounds request/response/header time, clears `GetBody`, signs timestamp/body and returns only sanitized receipt metadata. | RFC 9421 explains why replacing a signature format needs receiver verification/replay semantics. The IETF idempotency draft confirms the receiver, not the client, owns deduplication semantics. The SSRF taxonomy and AutoGPT advisory support the existing no-redirect/connection-time DNS design. | **not adopted**: RFC 9421 migration, client-side retry loop, or exactly-once claim. The current adapter deliberately makes one POST attempt; network ambiguity remains an upper-layer/receiver concern, while the stable `Idempotency-Key` and timestamp/body HMAC give the receiver a deduplication/replay hook. | No real notification is sent by tests. Preserve the configured-endpoint-only resolver, current tenant/channel checks, fixed error classifications and response-body discard. A client cannot prove the receiver validates freshness, signature or idempotency. |
| M10 model settings and credential cache | `adapter/modelsettings/store.go:16,31-49` has the one deployment-global `llm` record, not a tenant/principal-keyed setting. `app/modelsettings/service.go:59-179` requires platform admin. `adapter/sql/settings/store.go:151-215` encrypts optionally and CASes ciphertext; `modelruntime/registry.go:20-28` excludes the API key from plans/revisions. `CachedRepository` has a documented local five-second TTL (`cache.go:11-167`); `compiler.go:37-42,176-200` intentionally lets in-flight calls retain their adapter. | Google Secret Manager rotation material distinguishes disablement and rotation from cache policy. It is relevant to the tradeoff, not proof that this deployment-global setting must have tenant partitioning or distributed invalidation. | **reviewed; no confirmed exposure bug. pending product decision:** two cache instances over a local durable fixture can reproduce A returning K1 for up to five seconds after B saves or clears K1. A smallest no-schema alternative is to omit `CachedRepository` at server composition, at the cost of a DB read per resolution; it does not stop in-flight adapters. Distributed revision/invalidation requires a new protocol. | Keep the explicit five-second cache contract until product semantics say key clear/rotation must revoke new requests immediately. Recognition preview remains an intentional fixed mask, not raw secret exposure. |
| M36 identity, accounts and task authority | `storage/sql_accounts.go:313-343` changes the bcrypt hash and deletes every bearer token for an account in one transaction. Native-static execution projection can reconcile a pure account-epoch lag (`server_execution_projection.go:341-354,406-459`), but that static-authority refresh is not a durable-task revocation protocol. | RFC 9700 distinguishes token replay/revocation concerns, but this application is not an OAuth implementation and the RFC cannot define its task semantics. | **reviewed; no task-authority semantic change adopted.** The explored password/epoch bump was withdrawn: revoking bearer sessions is not equivalent to cancelling or invalidating durable tasks, and automatic native reconciliation could keep an already-admitted task running. A task-authority revocation contract must be designed independently. | Do not describe the withdrawn experiment as a fixed vulnerability. Preserve atomic bearer-token revocation and the existing reconciliation contract; do not bolt task cancellation onto password rotation. |
| M39 evaluation Store invariants | The prior Memory and SQL stores accepted a direct invented case, score `1`, and `passed=true`, letting `RequirePassed` succeed without proving immutable dataset membership. The runner (`evaluation/runner.go:193-245`) already owns known-case checking and aggregation, but direct Store callers lacked the equivalent boundary. This batch binds `CreateRun` to the stored dataset ID/version/revision and full count; permits an explicit valid run-profile override; validates member cases; and makes persisted case rows authoritative at finish. SQL `GetRun` also rejects legacy rows with missing datasets, foreign cases, or incomplete completed sets. | Anthropic's 2026 evaluation article supports deterministic outcome evidence and high-integrity regression gates. This is Store integrity, not a model-quality score or a claim about the article's benchmarks. | **implemented and accepted.** New records require a running bound run; canonical-equal result replay is a no-op even after terminal state. `FinishRun` accepts empty cases or an order-independent exact supplied/persisted case map, requires the full dataset set only for `completed`, permits failed partial sets, preserves stored timestamps, and uses existing terminal-field replay semantics. Review also found that direct Memory terminal assignment admitted `NaN`; shared `ValidateRunResult` now rejects non-finite scores before mutation. | Do not recompute scores/weights or alter terminal ownership in the Store, add a public API/schema, constrain a valid run-profile override to the dataset default, or reinterpret the Store as a score aggregator. SQLite/PostgreSQL conformance, clean strict PostgreSQL, full non-PG tests, coverage, and full non-PG race are recorded as final acceptance evidence. |
| M40 release, canary and rollback | `evaluation/types.go:170-400` has deterministic pass/min-score/max-regression gates. `control/canary.go:282-334,354-447,450-486` stages only a passed gate, uses stable tenant/subject bucketing, and promotes with baseline recheck; `storage/canary.go:99-138` conditionally transitions status and bumps revision/epoch transactionally. | OTel's 2025 CI/CD work supports release/VCS observation, not statistical automatic mutation. | **reviewed; not adopted:** automatic promotion/rollback from live failure rate. **pending measurement:** a read-only offline health report with predeclared sample/denominator/error-class/latency rules, where insufficient or missing data is inconclusive and never changes a canary. | Preserve human-controlled promotion, stable bucketing, gate/baseline checks and idempotent canary ID. Do not infer a statistically valid threshold from the present trace-only candidate correlation. |
| M43 telemetry, logging and build information | `core/telemetry.go:47-55,60-64,110-176,211-295` bounds correlation attributes and excludes inherited high-cardinality metric attributes. `otel.go:39-94` fixes instrument names. Before this batch, `spanEnder.End` bounded only status but `RecordError` wrote the full event message. `otel.go:131-167` now emits the standard `exception` event with the SDK-compatible type and an UTF-8-safe 1024-byte message. `logging` reads trace context only; `buildinfo` is logged but is not yet a telemetry resource attribute. | For this adapter's `End(err)` failure path, OTel exception guidance supports a standard `exception` event with Error span status; the tracing API identifies RecordException as a specialized event. This does not claim every handled exception must set Error. CI/CD OTel material supports later release correlation but not a public GenAI-field expansion. | **implemented; final gates passed:** local in-memory exporter regression covers 1023/1024/1025 bytes, a split-rune boundary, value/pointer error type parity with SDK `RecordError`, and nil. **not adopted:** a generic scrubber, secret policy, build-resource mapping, or GenAI semantic-convention fields. | The 1024-byte cap is a payload bound, not secret redaction. Callers must not put credentials in errors; retain standard error-type aggregation and no stacktrace unless a future contract opts in. |
| M44 test evidence and architecture tooling | `scripts/test-postgres/main.go:31-110` now requires each named `(Package, Test)` to have one run and one terminal event, rejects orphan/duplicate/out-of-order/unfinished lifecycles, and deliberately ignores legal package-level events. `:150-152` creates JSONL with `O_WRONLY|O_CREATE|O_EXCL`, mode 0600. Tests cover prior false-green streams and byte preservation after a second writer fails; `docs/architecture.md:128` and `docs/superpowers/sql-migration-verification-matrix.md:71-82` require a new evidence path each run. `internal/modulecheck/core_budget_test.go:18-149` enforces Core file/line/API budgets but currently treats any filename containing `generated` as generated. `internal/perfp0/harness.go:100-105,405-407` reports attempt throughput separately from errors. `verify-openapi/main.go:44,70-120,203-224` validates current literal top-level mux registrations and contract shapes. | Go test documentation is the runner parser-contract source; Anthropic's 2026 infrastructure-noise article says benchmark configuration can change observed results, so it should be recorded/controlled rather than silently reinterpreted. Anthropic's evaluation article motivates evidence integrity but makes no claim about this runner's model quality. | **implemented; final gates passed:** parser/file tests and package race/vet are green; the clean strict PostgreSQL runner independently verified every named lifecycle. **pending, not implemented:** require the canonical generated-file marker rather than a filename substring; clarify `perfp0` as attempt throughput requiring `Errors == 0` for comparisons. **reviewed; no current defect:** verify-openapi covers current route forms; dynamic/nested registration would need a separately reproducible false-green before a parser/framework change. | Do not accept interleaved/truncated evidence, turn package events into named-test terminals, or reuse an evidence filename. Do not call a possible modulecheck filename bypass or a successful-attempt metric an implemented defect. No production runtime, schema, Core or public API change. |
| M35 artifact migration | `objectstore.go:54-100,514-640` bounds/syncs temp streams and rechecks commit CAS; `s3client.go:377-473` spools SHA-256 with a 5 GiB limit and cancellation cleanup; migration/reconcile code validates size/digest and uses finite activation barriers. | Current S3 checksum material supports existing digest proof; 2024 object lock illustrates a provider feature that conflicts with portable cleanup. | **pending measurement**: quantify stale conditional-write waste before considering an optional preflight. **not adopted**: object lock/soft delete. | Commit-time CAS remains mandatory; a preflight may add I/O and cannot replace it. |
| M37 settings and secret view | Production bootstrap requires a master key before configuration read. SQL settings uses ciphertext-guarded CAS; storage configuration swaps resources by desired revision while active sessions intentionally retain captured backends until restart. | Current AWS/Vault cache material warns that refresh caches can retain stale or revoked values. | **not adopted**: a TTL or client secret cache without a revision and permission-revocation feed. | No independent secret-value cache exists today, so there is no cache-invalidation defect. Admin recognition previews are deliberate bounded views, not raw secret exposure. |

## First-pass, source-informed candidates

### R-01 — adapter contract scenarios, test-only

**Current evidence.** `pkg/app/modelexecution/stream.go` already rejects
post-finish events, duplicate usage, conflicting tool fragments, over-limit
arguments, and invalid finish/tool combinations. `registry.go` applies that
validator to every protocol event. The OpenAI, Anthropic and core bridge
adapters each have their own decoder tests, but there is no named cross-adapter
scenario table that expresses the same tool trajectory and expected normalized
event/final state for every built-in protocol.

**Candidate.** Add a package-level test scenario table, not a new runtime
abstraction: seed a synthetic provider transcript, execute each built-in
protocol through the existing registry, and assert normalized text/tool-call
ordering, exact usage handling, terminal reason, and the durable session effect
after a local executor consumes it. Structure each scenario as `init`,
`solution`, `assert`, following the useful *test construction* idea from
tau2-bench. Include malformed-stream negatives beside valid transcript cases.

**Why it is verifiable.** It runs with `httptest`/synthetic providers and local
fixtures, has no provider billing, and can be accepted only if all existing
adapters preserve their individual tests plus the shared outcomes.

**Status.** **pending design review**; no code change proposed in this ledger.

### R-02 — paired tool-output security contracts, test-only first

**Current evidence.** `pkg/core` resolves capabilities and policies;
`pkg/server/server_tool_checkpoint.go` records an effect admission before a
non-approval tool effect; `pkg/core`/`pkg/storage` preserve invocation state;
`pkg/execution/mcp_*.go` and `pkg/extensions/toollib` expose external tool
content and progressive disclosure; `pkg/execution/sandbox` and
`pkg/adapter/sandboxexec` enforce explicit assurance, network and resource
contracts. These are strong enforcement seams, but the current suite has no
small, shared, named corpus of untrusted tool-result prompt-injection cases
whose success criterion includes both a useful benign operation and the absence
of an unauthorized irreversible effect.

**Candidate.** Add a local deterministic fixture corpus at the server/execution
test boundary: initialize a benign requested action and an untrusted tool
payload, permit an authorized safe/read action, then assert final durable state
and journal effects. Pair each injection case with a benign counterpart using
the same capability surface. Include approval-required and sandbox network
denial cases. This mirrors AgentDojo's paired utility/security measurement
without importing its business domains or benchmark score.

**Why it is verifiable.** Local capabilities count effects; session/journal
records prove no hidden replay; sandbox probes are synthetic. A candidate fails
if it blocks the benign counterpart, so it cannot improve a headline security
number by becoming deny-all.

**Limit of the test.** A scripted local model can only establish that the
deterministic capability, approval, journal and sandbox enforcement seams make
the expected decision for a supplied payload. It cannot measure a model's
ability to recognize or resist prompt injection. Any future model-behaviour
claim needs a separately designed, provider-aware evaluation and must not be
derived from this deterministic gate.

**Status.** **pending design review**; no production policy, classifier, taint
model, or Core API change is proposed.

## Explicit non-adoptions in this pass

1. Do not import tau2-bench or AgentDojo as a production dependency, business
domain, task dataset, or score target. Their useful contribution here is a
test-design discipline, not an architectural dependency.
2. Do not add an LLM-based prompt-injection classifier into the tool path. It
would add latency, nondeterminism and a billing dependency at an authorization
boundary; existing deterministic policy, approval, capability and sandbox
contracts remain the enforcement point.
3. Do not weaken sandbox assurance to claim isolation when a provider reports
only process/shared-kernel or host networking. The current explicit assurance
and requested-network contract is the correct fail-closed boundary; new
providers need platform-specific evidence, not a generic fallback.
4. Do not create a cross-provider runtime adapter framework before a failing
shared scenario demonstrates an incompatibility. The current normalized
`modelexecution` contract and small built-in adapters preserve the open-source
public surface better.

## Next review sequence

Continue source comparison one M row at a time, preserve this ledger's
`pending` labels until code and a source are actually reviewed, and add a dated
measurement before changing a reviewed status to verified. Any future
implementation proposal must name its package owner, test command, expected
effect/failure boundary, and whether it consumes a Core/API budget.
